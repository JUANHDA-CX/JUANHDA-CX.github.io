<!DOCTYPE html>
<html>
<head>
<title>Demo Modal</title>
<style>
/* Estilos para el fondo oscuro */
[popover]::backdrop {
  background-color: rgba(0, 0, 0, 0.7);
  opacity: 0;
  transition: opacity 0.3s ease-in-out;
}

[popover]:open::backdrop {
  opacity: 1;
}

/* Estilos para el contenido del modal */
[popover] {
  border: none;
  background-color: #fff;
  padding: 20px;
  border-radius: 5px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.25);
  margin: auto;
  
  /* Posicionamiento y animación de entrada */
  transform: translateY(100%);
  animation: slide-in 0.3s forwards;
  
  /* Desactiva el posicionamiento por defecto del popover */
  inset: 0;
  width: min(90vw, 400px);
  height: fit-content;
}

/* Animación de entrada */
@keyframes slide-in {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}
</style>
</head>
<body>

<h1>Demo de Modal con :invokers</h1>



<a href="www.3djuegos.com">3d</a>

<button popovertarget="miModal">Abrir Modal</button>

<div id="miModal" popover class="modal">
  <h2>Bienvenido</h2>
  <p>Este es un ejemplo de modal en HTML</p>
  <button popovertarget="miModal">Cerrar</button>
</div>

</body>
</html>